import { NextPage } from 'next';

interface Props {}

const Projects: NextPage<Props> = () => {
    return (
        <div>
            <h1>Projects</h1>
        </div>
    );
};

export default Projects;
